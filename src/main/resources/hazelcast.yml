hazelcast-client:
  cluster-name: hazelcast-demo
  instance-name: hazelcast-client
  network:
    cluster-members:
      - 127.0.0.1
    auto-detection:
      enabled: false
  connection-strategy:
    async-start: true
    reconnect-mode: ASYNC